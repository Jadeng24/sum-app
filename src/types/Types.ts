// eslint-disable-next-line no-shadow
export enum Pages {
    Home = 'home',
    About = 'about',
    Shop = 'shop',
    BoatBuilder = 'boat-builder',
    Contact = 'contact',
}

export interface productColor {
    name: string;
    color: string;
}
