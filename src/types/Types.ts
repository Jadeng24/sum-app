// eslint-disable-next-line no-shadow
export enum Pages {
    BoatBuilder = 'boat-builder',
    Boats = 'boats',
    Contact = 'contact',
    Engines = 'engines',
    Home = 'home',
    Shop = 'shop',
}

export interface productColor {
    name: string;
    color: string;
}
